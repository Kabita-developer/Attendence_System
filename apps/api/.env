NODE_ENV=development
PORT=4000

# Frontend origin for CORS (Next.js dev server)
WEB_ORIGIN=http://localhost:3000

MONGODB_URI=mongodb://127.0.0.1:27017/attendance_system

# Used for signing short-lived JWT access tokens (stored in HTTP-only cookie)
JWT_ACCESS_SECRET=5b6604b0a72c7ad692432fc91b23190de5388294d4dba745915df4b16b648986
JWT_ACCESS_EXPIRES_IN=365d

# Persistent login tokens (stored in DB hashed; raw stored in HTTP-only cookie)
LOGIN_TOKEN_TTL_DAYS=30

# Cookie settings
COOKIE_DOMAIN=
COOKIE_SECURE=false
COOKIE_SAMESITE=lax

# App timezone (used for "once per day" attendance + reports)
APP_TIMEZONE=Asia/Kolkata

# Admin bootstrap (seed)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=Admin@12345

 ADMIN_SETUP_KEY=admin_setup_key


